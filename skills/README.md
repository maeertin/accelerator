# Skills

This folder contains installable skills for the published packages in `packages/*`.

These skills are intended for public consumption and help AI assistants use the packages from this repository in other projects.

Install them with:

```sh
npx skills add noaignite/accelerator
```

Each skill should describe the published npm package and how to use it from a downstream repository.
